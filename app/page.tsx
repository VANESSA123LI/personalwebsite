import Desktop from "./desktop/Desktop";

/*
 * Home page = the macOS desktop. All content and configuration lives in
 * app/desktop/config.ts; the previous text home page remains in git history.
 */
export default function Home() {
  return <Desktop />;
}
